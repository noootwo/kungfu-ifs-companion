# 0.1.1

## 变更内容

- 调整一条实用帮助规则，避免 Hermes Skills Guard 将普通写作误判为上下文外泄模式。
- 增加适用于不接受 `.tar.gz` 上传平台的 ZIP 发布包。
- SkillHub ZIP 包排除平台不接受的无扩展名 `LICENSE` 和 `VERSION` 文件；源码和 TAR 包仍保留 MIT 许可证及版本元数据。
- Skill 的陪伴行为、安全边界和参考资料结构保持不变。

## 验证命令

```bash
./scripts/validate.sh
./scripts/test_hermes_layout.sh
./scripts/package.sh
```

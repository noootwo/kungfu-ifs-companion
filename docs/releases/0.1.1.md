# 0.1.1

## Changes

- Clarified one practical-help guideline so Hermes Skills Guard does not confuse ordinary writing with a context-exfiltration pattern.
- Added a ZIP release artifact for marketplaces that reject `.tar.gz` uploads.
- The SkillHub ZIP omits only the extensionless `LICENSE` and `VERSION` entries, which that upload form rejects; the source and TAR archive retain MIT licensing and version metadata.
- Kept the skill behavior, safety boundaries, and reference structure unchanged.

## Validation

- `./scripts/validate.sh`
- `./scripts/test_hermes_layout.sh`
- `./scripts/package.sh`
